anytime_server:
  ros__parameters:
    # Anytime algorithm mode
    is_reactive_proactive: "reactive"  # Options: "reactive", "proactive"

    # Threading configuration
    multi_threading: false  # Enable/disable multi-threading

    # Batch processing configuration
    batch_size: 1024  # Number of RRT* iterations to compute per batch

    # RNG configuration
    random_seed: 42  # Seed for reproducible experiments

    # Logging configuration
    log_level: "debug"  # Options: "debug", "info", "warn", "error", "fatal"

    # TODO: RRT* specific parameters (to be finalized - see plan.md)
    # step_size: 0.05
    # goal_threshold: 0.05
    # rewire_radius: 0.1
    # map_file: ""
    # start_x: 0.0
    # start_y: 0.0
    # goal_x: 1.0
    # goal_y: 1.0
