anytime_server:
  ros__parameters:
    # Anytime algorithm mode
    is_reactive_proactive: "reactive"  # Options: "reactive", "proactive"
    
    # Threading configuration
    multi_threading: true  # Enable/disable multi-threading
    
    # Block processing configuration
    block_size: 1  # Number of iterations to compute per block
    
    # Synchronization mode
    is_sync_async: "sync"  # Options: "sync", "async"
    
    # YOLO weights path (empty means use default from package share directory)
    weights_path: ""  # Path to the anytimeyolo weights directory
    
    # Logging configuration
    log_level: "info"  # Options: "debug", "info", "warn", "error", "fatal"
